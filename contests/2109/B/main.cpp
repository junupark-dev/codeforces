#include <bits/stdc++.h>

using namespace std;

void solve();

int main() {
  int t;
  cin >> t;
  while (t--)
  solve();
  return 0;
}

void solve() {
  int n, m, a, b;
  cin >> n >> m >> a >> b;
  int u_bound = 0, l_bound = 0, d_bound = n - 1, r_bound = m - 1, i = a, j = b;
  while (u_bound != a && d_bound != b && l_bound != b && r_bound != b) {
    
  }
}
